// ============================================================
// page.tsx — The only page in this app.
// All it does is render the WallCalendar component.
// ============================================================

import WallCalendar from "./components/WallCalendar";

export default function HomePage() {
  return <WallCalendar />;
}
